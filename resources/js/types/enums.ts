// Enum types
export enum MovieGenreEnums {
    Animation = 'ANIMATION',
    // Add other genres if needed
}

export enum MovieLanguageEnums {
    Japanese = 'JAPANESE',
    // Add other languages if needed
}
